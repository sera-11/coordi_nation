<p style="color: green"><%= notice %></p>

<div class="p-5 mt-5">
  <%= render @event %>

  <div>
    <%= link_to "Edit this event", edit_event_path(@event) %> |
    <%= link_to "Back to events", events_organization_path(@event) %>

    <%= button_to "Destroy this event", @event, method: :delete %>
  </div>

</div>
